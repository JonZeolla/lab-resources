---
# The AppStream module stream on RedHat family. A stream is a major line, not
# a build: every converge takes the newest build that stream carries, so it is
# the image snapshot that fixes the exact version.
nodejs_major_version: "22"

# Exact pin for the Windows MSI; there is no stream to follow there.
nodejs_windows_version: "22.23.1"
